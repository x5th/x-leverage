[package]
name = "xleverage-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
# Program dependencies removed due to Anchor entrypoint conflicts in tests
# For testing, use `anchor test` command instead
# anchor-lang = { workspace = true }
# solana-program-test = { workspace = true }
# solana-sdk = { workspace = true }
# anyhow = { workspace = true }
# serde = { workspace = true }

[lib]
path = "src/lib.rs"

# Integration tests disabled due to Anchor entrypoint conflicts
# Uncomment and implement properly using solana-program-test when ready
# [[test]]
# name = "integration"
# harness = true

